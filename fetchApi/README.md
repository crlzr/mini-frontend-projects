## API to Use

Use the public JSONPlaceholder API:

Endpoint: https://jsonplaceholder.typicode.com/users

This returns a list of fake users in JSON format.

## Requirements

- Create a simple HTML page.
- Use fetch() to request the users from the API.
- Convert the response to JSON.

#### Display each user's:

- Name
- Email
- City

Handle loading and error states.